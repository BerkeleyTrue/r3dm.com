/** @jsx React.DOM */
'use strict';
var React = require('react');

var Connect = React.createClass({
  render: function() {
    return (
      <div className="connect con">
      </div>
    );
  }
});
module.exports = Connect;
