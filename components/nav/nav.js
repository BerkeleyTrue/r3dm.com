var React = require('react');

var Nav = React.createClass({displayName: 'Nav',
  render: function() {
    return (
      React.createElement("div", null, 
        "Nav"
      )
    );
  }
});

module.exports = Nav;
